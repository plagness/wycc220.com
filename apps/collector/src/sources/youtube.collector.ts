import type { SourceCollector } from "../collector.js";

export const youtubeCollector: SourceCollector = {
  source: "youtube",
  async *collect() {
    // Integration starts only after API terms, retention, quotas and deletion are approved.
  },
};
