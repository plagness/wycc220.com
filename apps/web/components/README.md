# Application components

Сюда помещаются композиционные компоненты сайта, использующие несколько UI-примитивов, но не принадлежащие одному домену. Универсальный примитив переносится в `packages/ui`; компонент одного раздела остаётся в `features/<domain>`.
